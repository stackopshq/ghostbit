# Pinned exactly: this file is what the Docker image installs, and an image
# that floats to "whatever PyPI served that day" is neither reproducible nor
# scannable (Trivy matches advisories against ==, not >=). These are the
# versions CI and the 2026-08-31 audit ran against. pyproject.toml keeps the
# loose lower bounds for editable installs and tooling.
fastapi==0.141.1
slowapi==0.1.10
uvicorn[standard]==0.52.4
jinja2==3.1.6
python-multipart==0.0.32
aiosqlite==0.22.1
redis[asyncio]==8.1.0
cryptography==50.0.1
argon2-cffi==25.1.0
pydantic-settings==2.15.0
pygments==2.21.0
prometheus-client==0.26.0
